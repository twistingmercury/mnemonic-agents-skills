apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "{{PROJECT_SLUG}}.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.37
      command: [wget]
      args: ["{{ include "{{PROJECT_SLUG}}.fullname" . }}:{{ .Values.service.port }}/healthz"]
